# Minimum versions. Unpinned upper bound so prebuilt wheels are available
# across Python versions (pinning pydantic too low forces a Rust source build
# on very new Python releases).
fastapi>=0.115
uvicorn[standard]>=0.30
SQLAlchemy>=2.0
httpx>=0.27
python-dotenv>=1.0
pydantic>=2.9
pydantic-settings>=2.4
